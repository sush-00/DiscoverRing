<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DiscoverRing" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">@color/yellow</item>
        <item name="android:itemBackground">@color/black</item>
    </style>

    // toolbar theme
    <style name="ToolbarStyle" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.DiscoverRing" parent="Base.Theme.DiscoverRing" />
</resources>